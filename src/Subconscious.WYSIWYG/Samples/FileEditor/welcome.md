# File Editor Sample

Markdown files are displayed as rendered content without exposing Markdown syntax.

- `calculator.cs` exercises the code editor.
- `settings.json` exercises JSON highlighting.
- `notes.txt` exercises plain-text editing.
