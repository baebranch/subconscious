// Dummy Program.cs to satisfy the compiler when using Microsoft.NET.Sdk.Web
// This project is a library, not an executable, so this is just a placeholder.

namespace Subconscious.Engine;

internal static class Program
{
    // This method is never called - it's just here to satisfy the compiler
    private static void Main()
    {
    }
}
